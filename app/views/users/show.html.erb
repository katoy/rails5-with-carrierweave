<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Body:</strong>
  <%= @user.body %>
</p>

<p>
  <strong>Image:</strong>
  <%= image_tag @user.image_url %>
  <div><%= @user.image_url %> on s3 </div>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
